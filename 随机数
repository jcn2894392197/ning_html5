<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<script src="./vue.js"></script>
		<title></title>
	</head>
	<body>
		<div id="app">

			<p v-if="num >= 90">{{num}}分，成绩优秀</p>
			<p v-if="num1 >=0 && num1 < 90">{{num1}}分，成绩合格</p>
			<p>我是一个[0-9]随机数：{{random_num}}</p>
			
			
		</div>
		
		<script>
		        let f = new Vue({
		            el: '#app',
		            data() {
		                return {
		                    num: 90,//分数
							          num1:80,
							random_num:Math.floor(Math.random()*10)
							// Math.floor()函数表示向下取整数
		                }
		            },
		            
		        })
		    </script>
	</body>
</html>
